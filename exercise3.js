function sum(a, b) {
    return a + b;
}
function fixedNumber() {
    return 20;
}
function formatedString(input, isUpperCase) {
    if (isUpperCase) {
        return input.toUpperCase();
    }
    else {
        return input.toLowerCase();
    }
}
